const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) @const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) @const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) @const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) -const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 0const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 1const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 6const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) jconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) %const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 0const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 9const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) %const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 2const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) %const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 2const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) —const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ^const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ^const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) @const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) +const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) @const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ^const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) @const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) +const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ^const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) @const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) +const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) $const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) jconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) úconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ñconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) *const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ñconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) áconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) éconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) qconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) —const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) qconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) —const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) !const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) óconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) áconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) <const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) óconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) >const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) &const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) &const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) |const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) `const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) -const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) $const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) `const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) _const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) —const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) [const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ]const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ,const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) {const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) óconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) éconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) áconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) -const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 0const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 1const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 6const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) vconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) —const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) kconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) \const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) %const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 2const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) %const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 2const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Lconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) yconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) =const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) gconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) .const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) mconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) uconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) fconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) dconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) iconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) cconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) Pconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) aconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) hconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) (const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ?const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) nconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) xconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) tconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) :const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password)  const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) /const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) bconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) rconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) oconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) wconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) sconst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) econst password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 'const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) )const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) ;const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) }const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 
const password = String(formData.get('password') ?? '');\n    const captchaToken = String(formData.get('captchaToken') ?? '');\n    const hcaptchaEnabled = Boolean(getOptionalApiKeys().hcaptchaSiteKey);\n\n    if (!identifier || !password) 